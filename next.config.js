/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['github.com', 'avatars.githubusercontent.com', 'opengraph.githubassets.com'],
  },
}

module.exports = nextConfig